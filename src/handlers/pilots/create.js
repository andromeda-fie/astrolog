/**
 * TODO: Fix this `any` into an actual type
 * @param {any} data
 * @returns {import('../../types/result.d.ts').Result}
 */
export function createPilot(data) {
    return { type: 'ok', data: [] }
}
